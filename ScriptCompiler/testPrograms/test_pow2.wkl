
def _hit_(){
    in damage, dmgType;
    
    in target, missChance, critChance;
    
    (result, amount) = doPhycicalDamage(damage, dmgType);

}
    
     
def doPhycicalDamage(damage, dmgType){
    in target, missChance, critChance;

    target.hp = target.hp - 100;
    
    return (1, 10);
}
